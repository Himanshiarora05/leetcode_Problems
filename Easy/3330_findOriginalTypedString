class Solution {
public:
    int possibleStringCount(string word) {
        int n = word.length();
        int count = 1;   //if nothing is mistyped or repeated 
        for(int i = 1;i<n;i++){
            if(word[i]==word[i-1]) count++;
        }
        return count;
    }
};
